# EKS Auto Mode NodeClass for GPU instances
# LIMITATION: userData for GPU time-slicing is NOT supported in Auto Mode.
#   The original blueprint configured NVIDIA GPU time-slicing via Bottlerocket userData.
#   Auto Mode does not support custom userData/bootstrap scripts.
#   GPU time-slicing must be configured through other means (e.g., NVIDIA GPU Operator).
#
# Note: When using a custom NodeClass in Auto Mode, an EKS Access Entry is required:
#   aws eks create-access-entry --cluster-name <cluster-name> --principal-arn <node-role-arn> --type EC2
#   aws eks associate-access-policy --cluster-name <cluster-name> --principal-arn <node-role-arn> \
#     --policy-arn arn:aws:eks::aws:cluster-access-policy/AmazonEKSAutoNodePolicy --access-scope type=cluster
apiVersion: eks.amazonaws.com/v1
kind: NodeClass
metadata:
  name: gpu-bottlerocket
spec:
  # amiSelectorTerms: removed — Auto Mode manages AMI selection
  # role: removed — Auto Mode manages node IAM role via access entries
  # userData: removed — NOT SUPPORTED in Auto Mode (was used for GPU time-slicing config)
  securityGroupSelectorTerms:
    - tags:
        karpenter.sh/discovery: "<<CLUSTER_NAME>>"
  subnetSelectorTerms:
    - tags:
        karpenter.sh/discovery: "<<CLUSTER_NAME>>"
  tags:
    intent: gpu-workloads
    karpenter.sh/discovery: "<<CLUSTER_NAME>>"
  ephemeralStorage:
    size: "20Gi"
