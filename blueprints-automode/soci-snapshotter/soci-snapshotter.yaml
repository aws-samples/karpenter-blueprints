# LIMITATION: soci-snapshotter EC2NodeClass used userData for SOCI snapshotter configuration
#   and instanceStorePolicy: RAID0. Both are NOT supported in Auto Mode.
#   userData is required for SOCI snapshotter installation — this NodeClass is NOT fully migratable.
# Note: When using a custom NodeClass in Auto Mode, an EKS Access Entry is required:
#   aws eks create-access-entry --cluster-name <cluster-name> --principal-arn <node-role-arn> --type EC2
#   aws eks associate-access-policy --cluster-name <cluster-name> --principal-arn <node-role-arn> \
#     --policy-arn arn:aws:eks::aws:cluster-access-policy/AmazonEKSAutoNodePolicy --access-scope type=cluster
apiVersion: eks.amazonaws.com/v1
kind: NodeClass
metadata:
  name: soci-snapshotter
spec:
  # amiSelectorTerms: removed — Auto Mode manages AMI selection
  # role: removed — Auto Mode manages node IAM role via access entries
  # instanceStorePolicy: removed — NOT SUPPORTED in Auto Mode
  # userData: removed — NOT SUPPORTED in Auto Mode (was used for SOCI snapshotter config)
  # blockDeviceMappings: removed — converted to ephemeralStorage below
  securityGroupSelectorTerms:
  - tags:
      karpenter.sh/discovery: "<<CLUSTER_NAME>>"
  subnetSelectorTerms:
  - tags:
      karpenter.sh/discovery: "<<CLUSTER_NAME>>"
  ephemeralStorage:
    size: "100Gi"
---
apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: soci-snapshotter
spec:
  disruption:
    consolidationPolicy: WhenEmptyOrUnderutilized
    consolidateAfter: 5m
  template:
    metadata:
      labels:
        intent: soci-snapshotter
    spec:
      expireAfter: 336h
      nodeClassRef:
        group: eks.amazonaws.com
        kind: NodeClass
        name: soci-snapshotter
      requirements:
      - key: kubernetes.io/os
        operator: In
        values:
        - linux
      - key: eks.amazonaws.com/instance-category
        operator: In
        values:
        - c
        - m
        - r
      - key: kubernetes.io/arch
        operator: In
        values:
        - amd64
      - key: karpenter.sh/capacity-type
        operator: In
        values:
        - spot
        - on-demand
---
# LIMITATION: soci-snapshotter-br EC2NodeClass used userData for Bottlerocket SOCI snapshotter
#   configuration and instanceStorePolicy: RAID0. Both are NOT supported in Auto Mode.
#   This NodeClass is NOT fully migratable.
# Note: When using a custom NodeClass in Auto Mode, an EKS Access Entry is required:
#   aws eks create-access-entry --cluster-name <cluster-name> --principal-arn <node-role-arn> --type EC2
#   aws eks associate-access-policy --cluster-name <cluster-name> --principal-arn <node-role-arn> \
#     --policy-arn arn:aws:eks::aws:cluster-access-policy/AmazonEKSAutoNodePolicy --access-scope type=cluster
apiVersion: eks.amazonaws.com/v1
kind: NodeClass
metadata:
  name: soci-snapshotter-br
spec:
  # amiSelectorTerms: removed — Auto Mode manages AMI selection
  # role: removed — Auto Mode manages node IAM role via access entries
  # instanceStorePolicy: removed — NOT SUPPORTED in Auto Mode
  # userData: removed — NOT SUPPORTED in Auto Mode (was used for Bottlerocket SOCI config)
  # blockDeviceMappings: removed — converted to ephemeralStorage below (data partition size used)
  securityGroupSelectorTerms:
  - tags:
      karpenter.sh/discovery: "<<CLUSTER_NAME>>"
  subnetSelectorTerms:
  - tags:
      karpenter.sh/discovery: "<<CLUSTER_NAME>>"
  ephemeralStorage:
    size: "100Gi"
---
apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: soci-snapshotter-br
spec:
  disruption:
    consolidationPolicy: WhenEmptyOrUnderutilized
    consolidateAfter: 5m
  template:
    metadata:
      labels:
        intent: soci-snapshotter-br
    spec:
      expireAfter: 336h
      nodeClassRef:
        group: eks.amazonaws.com
        kind: NodeClass
        name: soci-snapshotter-br
      requirements:
      - key: kubernetes.io/os
        operator: In
        values:
        - linux
      - key: eks.amazonaws.com/instance-category
        operator: In
        values:
        - c
        - m
        - r
      - key: kubernetes.io/arch
        operator: In
        values:
        - amd64
      - key: karpenter.sh/capacity-type
        operator: In
        values:
        - spot
        - on-demand
---
# non-soci-snapshotter NodeClass — fully migratable (no userData)
# Note: When using a custom NodeClass in Auto Mode, an EKS Access Entry is required:
#   aws eks create-access-entry --cluster-name <cluster-name> --principal-arn <node-role-arn> --type EC2
#   aws eks associate-access-policy --cluster-name <cluster-name> --principal-arn <node-role-arn> \
#     --policy-arn arn:aws:eks::aws:cluster-access-policy/AmazonEKSAutoNodePolicy --access-scope type=cluster
apiVersion: eks.amazonaws.com/v1
kind: NodeClass
metadata:
  name: non-soci-snapshotter
spec:
  # amiSelectorTerms: removed — Auto Mode manages AMI selection
  # role: removed — Auto Mode manages node IAM role via access entries
  # blockDeviceMappings: removed — converted to ephemeralStorage below
  securityGroupSelectorTerms:
  - tags:
      karpenter.sh/discovery: "<<CLUSTER_NAME>>"
  subnetSelectorTerms:
  - tags:
      karpenter.sh/discovery: "<<CLUSTER_NAME>>"
  ephemeralStorage:
    size: "100Gi"
---
apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: non-soci-snapshotter
spec:
  disruption:
    consolidationPolicy: WhenEmptyOrUnderutilized
    consolidateAfter: 5m
  template:
    metadata:
      labels:
        intent: non-soci-snapshotter
    spec:
      expireAfter: 336h
      nodeClassRef:
        group: eks.amazonaws.com
        kind: NodeClass
        name: non-soci-snapshotter
      requirements:
      - key: kubernetes.io/os
        operator: In
        values:
        - linux
      - key: eks.amazonaws.com/instance-category
        operator: In
        values:
        - c
        - m
        - r
      - key: kubernetes.io/arch
        operator: In
        values:
        - amd64
      - key: karpenter.sh/capacity-type
        operator: In
        values:
        - spot
        - on-demand
